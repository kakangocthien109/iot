#include <stdio.h>

int main()
{
    int PrimeArr[50];
    PrimeArr[0] = 2;
    PrimeArr[1] = 3;

    int PrimeCount = 2;
    for(int i = 4; i <= 100; i++) {
        if (i % 2 = 1) {
            if (i )
        }
    }

    return 0;
}